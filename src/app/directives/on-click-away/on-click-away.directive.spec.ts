import { OnClickAwayDirective } from './on-click-away.directive';

describe('OnClickAwayDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickAwayDirective();
    expect(directive).toBeTruthy();
  });
});
